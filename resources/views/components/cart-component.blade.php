@auth
  <a class="fixed bottom-2 right-2 rounded-full bg-main shadow-lg" href="{{ route('cart') }}">
    <div class="relative p-4 px-5">
        <i class="fas fa-shopping-cart text-white"></i>
    </div>
  </a>
@endauth